; This job only talks to APIs (Bitbucket, Elasticsearch) — it doesn't
; configure a remote host, so it runs against localhost under the
; controller (or the CI runner) itself.
[local]
localhost ansible_connection=local
